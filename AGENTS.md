# Codex Project Rules

- Do not delete or rewrite existing working code unless the user explicitly asks for removal or the change is required to fix a concrete bug.
- Prefer additive changes. For styling changes, append overrides near the bottom of the relevant CSS file whenever practical.
- Keep unrelated local files out of commits unless the user specifically asks to include them.
- Preserve secrets by keeping `.env` and local logs out of Git.
